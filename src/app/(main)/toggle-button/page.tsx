import ToggleButton from '@/components/ui/toggleButton/ToggleButton';
import React from 'react';

const page = () => {
    return (
        <div>
            <ToggleButton/>
        </div>
    );
};

export default page;